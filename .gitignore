node_modules/
.next/
data/
.env
pbx.env
*.tsbuildinfo
__pycache__/
# Editor/OS cruft — must never be committed (a stray vim swap file was tracked
# once and showed up as a phantom deletion on every checkout).
*.swp
*.swo
*~
.DS_Store
# pbx/bootstrap-zeus-pbx.sh staging dir (rendered fragments, rebuilt each run)
.pbx-stage/
# Pinned full-stack build inputs (scripts/fetch-vendor.sh). ~40 MB of
# upstream artifacts — fetched, never committed; CI caches them.
/vendor/
